package automata.states;

public interface FSAState{

    /**
     * Return whether the state is an accepting state
     * @return whether the state is an accepting state
     */
    boolean isAccepting();
}
